<h1>Editing category</h1>

<% form_for(@category) do |f| %>
  <%= render f%>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @category %> |
<%= link_to 'Back', categories_path %>